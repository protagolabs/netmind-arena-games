/**
 * @arena/game-sdk — author custom Arena game types.
 *
 * ```ts
 * import { defineGame } from '@arena/game-sdk'
 * export default defineGame({ meta, init, play, apply, terminal, score, render })
 * ```
 */

export * from './types.js'
export * from './testkit.js'
